info:
  name: Update User
  type: http
  seq: 4

http:
  method: PATCH
  url: http://localhost:3000/api/users/4
  body:
    type: json
    data: |-
      {
        "email":"new.user@example.com"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
